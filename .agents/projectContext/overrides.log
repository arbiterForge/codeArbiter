# codeArbiter Override Log
# Format: [ISO-8601 timestamp] | BY: <name> <<email>> | PLATFORM: <github|gitea|unknown> | GATE: <gate> | REASON: <reason>
# This file is APPEND-ONLY. Entries are NEVER edited or deleted.
# Committed to repo — permanent audit artifact visible to all reviewers.
#
# FRAMEWORK-SOURCE INTENT: In the codeArbiter source repository (this file as
# shipped via vendor submodule), this log is intentionally EMPTY beyond this
# header. Consumer projects accumulate their own /override audit entries in
# their own ${PROJECT_ROOT}/.agents/projectContext/overrides.log, which is
# distinct from this framework-source copy. Framework-development bypass
# events (when the codeArbiter authors themselves invoke /override while
# editing the framework in self-edit mode) are documented in the relevant
# commit messages and `${FRAMEWORK_ROOT}/docs/` history, NOT here, to avoid
# polluting consumer audit trails with framework-internal bypass records.
