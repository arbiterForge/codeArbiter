name: Concepts source inputs
on:
  push:
    branches: [docs/concepts-execution-foundation]
    paths: [.github/workflows/concepts-source-inputs.yml]
permissions:
  contents: read
jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Capture source objects without executing project code
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/concepts-source"
          git bundle create "$RUNNER_TEMP/concepts-source/source.bundle" HEAD --tags ^b41d9803eacb3acf4b93c8bec81e1aa2f086aec7
          git rev-parse HEAD HEAD^{tree} > "$RUNNER_TEMP/concepts-source/identity.txt"
          cd "$RUNNER_TEMP/concepts-source"
          sha256sum source.bundle identity.txt > SHA256SUMS
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: concepts-source-inputs
          path: ${{ runner.temp }}/concepts-source/
          retention-days: 1
          if-no-files-found: error
