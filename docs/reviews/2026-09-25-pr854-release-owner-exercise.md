Unit: `pr854_d07_independent_release_owner`

Status: **DONE**. No actionable defect established in the bounded cases below. This is an independent agent traversal of the current release owner with real helper execution in newly authored scratch fixtures. It is not an invocation of a live Claude/Codex/Pi command loader or a new qualification of the entire historical release lane.

Candidate: `C:/Users/brenn/projects/codeArbiter-worktrees/pr854-stack-completion`. Immutable comparison: `30279f9df7650af5b2680d493aba7d34caeea004`. I read the release owner, generated public command, relevant helpers, and installed review contracts. I did not read the parent's proposed proof outcomes or edit candidate files. My fixtures, harness and artifacts are confined to `C:/Users/brenn/.codex/tmp/pr854-d07-independent-20260926`.

The canonical declaration is exactly `{{SKILL_ENTRY:release}}` plus newline. The owner discovery description and `[target] | --dry-run` hint changed. The canonical procedure body after frontmatter is byte-identical to the immutable prior, SHA256 `b87e885a55d097628a2d7bec17a63572800b78b6b760cbc7ae7f039ee6799cb7`. The generated public command and generated owner contain identical procedure bodies. The three exercised helper files are byte-identical to the immutable prior. These comparisons supplement the exercise; they do not establish behavior by themselves.

All seven candidate file identities were measured at the beginning and end, and were unchanged. The first four also match the separately captured first-inspection hashes.

| Candidate path | Bytes, start = end | SHA256, start = end |
| --- | ---: | --- |
| `core/surface/commands/release.md` | 24 | `38ea3697585f72a15d003be5be926090977586e7820311e8911a28575e5db509` |
| `core/surface/skills/release/SKILL.md` | 142447 | `41f0ba04d4192b0e8432e0f50377d9f01fcb0d6a1dd03b2db4155c91b3c1a421` |
| `plugins/ca/commands/release.md` | 142729 | `3219fda73142bbfcb50a82b6fc41d408abddb65528451fabf4a6cea33ffc060b` |
| `plugins/ca/skills/release/SKILL.md` | 142712 | `27732e78c17dbd20e3e37d0f794bf878102cbad1802ba0116871fb8e9f9a7285` |
| `plugins/ca/hooks/_releaselib.py` | 211124 | `9fdd234b7953dc0d232dbb4bc05c97253251b56953d74a76f26caaebd7dc1a64` |
| `plugins/ca/hooks/releasehash.py` | 11348 | `a9e806767403343937015026d544513985a6fb8d5cf7578f42d038797d9b0614` |
| `plugins/ca/hooks/_gitexec.py` | 2418 | `6dc323512f8a50b2a82a8ff63cd5eb2d1e98aefb9dafcc802b8053a2bc838d68` |

I created consumer and payload directories whose paths contain spaces, copied the exact generated owner/command/helpers, and used real Git for Windows Bash (`C:/Program Files/Git/bin/bash.exe --noprofile --norc`). The owner's Python selection resolved to CPython 3.14.6; Git was 2.55.0.windows.3. Child environments omitted provider credentials, disabled global/system Git configuration and hooks, and set the owner's no-bytecode/UTF-8 controls. The fixture had local tags and local tracking refs but no remote URL. No fetch, push, hosted command, provider API or publication was attempted.

The independently chosen fixture contained two targets, an atlas baseline tag `atlas-v1.4.2`, a higher unrelated beacon tag `beacon-v99.0.0`, two equal atlas manifests, declared generated/build artifacts, ordered pre-tag commands including a comma, an excluded atlas directory, and unrelated/excluded breaking commits without changelog footers. I selected and executed the owner's exact shell initialization, clean-tree and commit-path functions, and effective-window function, then assembled the selected read-only helper sequence from its procedure. There is no automatic end-to-end release CLI parser here: selecting the invocation, stopping on ambiguous target output and stopping at publication authority were this reviewing agent's decisions under the prose owner.

| Independently selected case | Observed result |
| --- | --- |
| Explicit `atlas` preview | Derived patch `1.4.3`; exactly the real selected fix commit was classified. The higher sibling tag, sibling breaking commit and excluded breaking commit did not affect the selected window. |
| Ordered pre-tag records | Preserved two commands and the comma inside the first command. Merely displayed them; did not execute them. |
| Preview effects | Complete file-content snapshots including `.git` were identical before/after; declared generate/rebuild/pre-tag/release-build sentinels did not appear. No approval marker, asset directory or bytecode cache appeared. The helper path cleaned its own external temporary pathspec file. |
| Omitted target, two targets | Helper enumerated `atlas` and `beacon`; this agent stopped for selection rather than guessing. |
| Omitted target, one target | Actual helper enumeration selected `atlas`, and its prefix lookup returned `atlas-v`. |
| Unknown target | Helper exit 2 with `unknown release target`; no fallback or fixture changes. |
| Default branch | Exact owner commit-path function refused `main`, exit 1; no fixture changes. |
| New real in-scope commit missing footer | Preview exited 1 before any version report; `[NEEDS-TRIAGE]` named that actual commit. No manifest, changelog, refs, index or configuration changes. |
| Real uncommitted selected-source dirt | Exact owner clean-tree function exited 1 and preserved the dirt. |
| Genuinely absent target declaration | `list-targets` exit 3; `backfill-detect` returned a proposal, without creating `.codearbiter`, persisting the declaration or recording confirmation. |
| Existing malformed declaration | Exit 4 `FileExistsNoBlockError`; not treated as absent and not repaired or overwritten. |
| Non-Git inspection failure | Real helper failed 128 and exact owner clean-tree function propagated 128. Empty stdout did not become success. |
| Missing commit-path prerequisite | Exact owner function refused absent `.codearbiter/tech-stack.md`, exit 1. |
| Synthetic publication-state tuples | Real classifier returned `publish_fresh`, `resume_publish`, `already_published`, and `abort_mismatch` for four independently selected cases. These labels establish classifier behavior only; they do not grant authorization or prove a hosted publisher. |

Publication authority was assessed from the owner and helpers: the owner reserves tag composition for a qualifying hosted publisher and explicitly says CI success alone does not authorize publication. `releasehash.py` handles command-confirmation mechanics, not publication authorization. I stopped before preparation writes and before every publisher operation; no absent-authorization bypass or credentialed publication was attempted.

The complete successful experiment was run with `C:/Python314/python.exe C:/Users/brenn/.codex/tmp/pr854-d07-independent-20260926/exercise.py`. The evidence contains 18 recorded non-setup invocations and 86 assertions including fixture setup and identity checks; these are not 86 independent product tests. The invocation transcript preserves argv, working directory, exit, stdout and stderr. `preview-lane.sh` preserves the exact assembled preview sequence. The helper commands exercised were `list-targets`, `show-row`, `tag-prefix`, `check-manifests`, `last-tag-for-policy`, `verify-tag-ancestor`, `payload-pathspec`, `classify-window`, `derive-version`, `version-greater`, `list-field`, `clean-tree-status`, `backfill-detect`, and `classify`, together with local read-only Git operations. The fixture setup commands are separately labeled in the transcript.

Earlier attempts are retained. The initial harness stopped while extracting an indented shell fence; attempt 02 used my invalid `release-assets` declaration key and the product correctly rejected it; attempt 03 expected exit 2 for a non-Git probe, whereas the product correctly propagated 128. I corrected the harness/fixtures, not candidate code, and reran the whole bounded experiment as attempt 04. The failed attempts are not product defects.

Explicit limits: no existing repository suite was used as the new exercise; no actual model request, live host loader or autonomous command-routing behavior was measured. Only Git for Windows Bash and CPython 3.14 were exercised. No full first-release derivation, calendar policy, provenance digest, releasehash approval workflow, build, asset upload, tag push, hosted exact-head CI, remote freshness, merge or public readback was exercised. Historical proof remains historical and is not reattested by this bounded result. Source-body equality does not renew unspecified historical scenarios. Any update to `agent-lane-proof.json` should name these cases and limitations, retaining historical evidence separately.

Evidence files, SHA256:

- `exercise-results.json`: `a944457531848663f6fec29319bf0365d13f859b5c55da4efee0894fef193100`
- `command-transcript.json`: `0781bd4dd54d75b28f4426e3f21ff111dd212ac6976d3181c0e32a5a6ebc6386`
- `check-transcript.json`: `45eb7c5a4d94031d7fa93abc71e95145cd9876f82637d010f1e71b0d5a495e94`
- `preview-lane.sh`: `05f181300b861987e4f0528bcbbd7361724b748e69e2dc0598035807de800051`
- Parent-directory `exercise.py`: `d40bfe6fa8a88a52592f4bd474f2683442522dd24fa0e1fc6faf64ce78beb946`
