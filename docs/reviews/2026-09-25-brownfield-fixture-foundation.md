# Brownfield continuation: CI closure and deterministic fixture foundation

Date: 2026-09-25. Campaign owner: PR #872, single native specification and plan at `4decde20df14cf4dfcd1e2762723772d1d5c8b53`. Implementation continuation: PR #873, parent `d4a9589b85fe56691f16b0eb5a63ab0a3e52cd1c`. Source baseline is `arbiterForge/codeArbiter@4187f1e22dc7da3cf755a2654bbf7ec268236126:/`.

## Delivered

The maintainer authorized continuation. This increment repairs the first slice's actual CI integration omissions and prepares the deterministic repository corpus used by campaign T-002. It does not claim a completed campaign checkpoint, native task acceptance, or exercise readiness. The canonical specification and plan are unchanged.

CI run 36183939006 failed on the parent. The actual causes were the stale README version badge, two native authoring tests absent from the exact conformance inventory, and a stale site context capture. The corrected conformance inventory adds those two tests without removing any existing obligation. The badge now agrees with the candidate manifest.

The site capture now separates its retained historical teaching baseline from the current helper identities. Current helper bytes are bound by SHA-256 and Git blob identities, with both checked against the working source and stored object. Blob identity survives a squash merge without depending on an ephemeral PR commit. All captured fixture observations remain unchanged; only source identity metadata changes. The browser evidence field and source link follow the same identity distinction.

CodeRabbit review comment 4108290980 was revalidated: the generated root package manifest is a declared release input and its version agrees with the Pi source manifest. Only its missed provenance hash was updated, through the existing provenance writer. No general hash reset or semantic freshness claim was made.

## Fixture foundation

A closed, bounded test-only catalog describes eleven synthetic repository archetypes. The materializer creates only absent test-owned targets, runs fixed Git operations, preserves staged versus working and ignored content, and rejects malformed/unsafe input. It executes no declared application, install, test or deploy command. The public runner rejects unknown and zero-test selections.

Evaluator records are outside the solver workspace and its Git history. Whole-case and task hashes prevent a changed task or dirty-state recipe from hiding behind an unchanged initial snapshot. These are identity and placement checks, not oracle approval, model correctness, or security confinement. A future runner must prevent access to evaluator siblings and this source catalog. No paid/model trial, consumer onboarding, or instruction-file publication occurred.

The suite is registered in the existing native hook-contract cells. Fixture-only path changes select it. All 59 prior non-guard command steps remain in order; one new step produces 60, with the exact partition digest and count updated. Deleting the new step fails the strict inventory. No deadline, assertion, skip, coverage threshold or safety gate was relaxed.

## Other work

The current #854 source was re-read at `2530d1ce55490a0647810eb271f34bbaa17869f1`. A disposable bounded overlay passes the nineteen fixture tests and eighteen partition tests. Its expanded native inventory retains thirteen farm cases; only the two authoring test names are added. This is targeted integration evidence, not a full merged-tree qualification or import of #854.

A later connector read found #854 at `a469bf51d65ecbd88fae9d21994470f63f78630c`. Its seven-path delta from the tested snapshot changes reconciliation source/generated references, their tests and review record; none of this continuation's fixture, CI-inventory or context-capture paths changed. Keep that independent correction. The overlay results remain explicitly bound to `2530d1c`, not relabeled as full qualification of the later head.

Merge-order overlap includes the earlier manifests/version identities/changelogs/provenance plus the CI workflow and conformance inventory. Preserve #854's complete suite memberships and workflow-owner changes; add this step and its trigger without replacing its workflow. Whichever PR lands second must recheck spent versions and reconcile current main. #871's debug design remains separate and untouched.

## Evidence and remaining work

The adjacent evidence file records exact source identities and executed checks, including failed runs and their dispositions. The broad initial conformance attempt used the assembled six-platform release payload where the package fixture expects one native candidate. That failed setup is not a product pass; the normal source-built single-cell conformance path is evaluated separately. An unchanged site test exceeded its existing five-second limit once; its isolated diagnostic and subsequent complete site pipeline passed without changing the test or timeout. The failure is retained, not declared fixed or attributed to infrastructure.

The long scratch path also exposed Git-for-Windows filename limits in an existing linked-worktree test. After enabling long paths only in the session-owned Git repository and removing the branch left by the failed test setup, the same CI-impact suite passed. No user/global Git or installed-host setting changed. A compound terminal inspection was blocked; it changed no repository data.

The bounded report decoder, resource accounting, governed context writer, initialization/recovery, new-input provenance, and live contribution journeys are still future campaign work. The engineering sign-off covers this tested source preparation and the repaired integration paths, not an independent policy-owned review or artifact approval receipt. No second spec or plan is introduced.

Rollback the new fixture runner/helper/catalog and their CI registration together. Keep the badge, source-capture, native-suite membership and root-provenance repairs with the parent runtime fix; reverting them alone would restore its integration failures. Preserve consumer state, historical assessment files and independent #854 work. Organization canon and cross-product claim registries are unchanged.
