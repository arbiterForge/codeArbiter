# Completeness review against the original specification

**No whole-feature acceptance is claimed.** These are author review notes, not independent approval. Design criteria and task definitions are retained under `design/original-review2/`.

| Criterion | Local evidence and outstanding review |
|---|---|
| **AC-001** No additional discovery surface | Locally exercised: the captured pre-engine command, agent, top-level-skill and persistent-tool sets are unchanged; startup fixtures are byte-bound and negative controls reject `/ca:spec`, a persistent artifact tool and eager schema loading. Hosted exact-head proof remains pending. |
| **AC-002** Explicit spec contracts and honest drafts | The 0.3.x content model differs from reviewed schema 0.2.0. Readiness checks presence/relationships, not prose truth; review every original content obligation. |
| **AC-003** Plan binding and bidirectional coverage | Local pair binding and coverage/DAG checks run. Existing plan-producing/consuming host workflows are not end-to-end qualified. |
| **AC-004** Stable symbol identity | Local stable IDs, retirement and duplicate authority checks run. Review rename/import behavior in the actual repository history. |
| **AC-005** Canonical HTML with detectable view drift | Normal engine reads reject model/view drift. Byte-preserving prior-candidate reads are tested; manual integration must not bypass the parser. |
| **AC-006** Offline branded human review | Offline Chromium and local branding tests are available. No cross-browser/native accessibility qualification or automatic repository branding-configuration discovery is claimed. |
| **AC-007** Bounded contextual reads | Whole-record contextual reads and forward progress are checked. Real-host context budget and complete dependency/use-site closure remain to review. |
| **AC-008** Typed operations and inert input | Closed inert operation protocol is exercised. Invocation through each host permission boundary is not fully wired. |
| **AC-009** Conflict-safe writes | Linux and Windows cooperative writer/CAS/replay/recovery tests run. macOS exact-host and physical power-loss/network-filesystem guarantees remain unqualified. |
| **AC-010** Repository and executable trust | Anchored Linux and root-handle Windows paths plus installation-pinned bridges pass locally. macOS and every real host permission boundary remain to qualify. |
| **AC-011** Approval is a governed transition | The host-owned prompt adapter binds an exact token reply to current content and drives capture plus approval in the installed-host workflow. SMARTS, reviewer and verification adapters remain unintegrated; test prompts are not production approval. |
| **AC-012** Changed contracts block stale execution | Local normative binding/freshness rejects stale content. Outer workflow resume and reapproval flows require target-host tests. |
| **AC-013** Evidence-backed task acceptance | Named-test checks and declared-task input coverage are strengthened. Actual runner adapters and complete dependency/environment input closure remain required. |
| **AC-014** Interruption-safe, deadlock-free scope execution | Engine scope transitions, stale revalidation, interrupted REVIEW resume and atomic scope acceptance run against a fresh native candidate. Full real-host caller checkpoint/resume behavior is not integrated. |
| **AC-015** Existing workflow end-to-end integration | Canonical pilot routing and synthetic native-candidate authoring/execution fixtures pass. Complete real-host feature/sprint traces and alternative paths remain open; do not report full criterion satisfied. |
| **AC-016** Explicit intent coverage without false proof | Structured scope links replace Markdown heuristics for typed specs. External issue-checkbox checking remains a heuristic and cannot prove intent completeness. |
| **AC-017** HTML discovery and correct cache invalidation | Candidate advisory hook wrapper and mocked seam tests only; run actual cache/dedup/receipt freshness suites on all generated hosts. |
| **AC-018** Derived farm compatibility | Locally implemented: minimal current-slice projection, source/spec/plan/slice/ID/base/RED binding, pre-canary source validation, provider-only enrichment, exact execution-byte/provider seals, and shared built-dispatcher enforcement all have fresh native-candidate fixtures. This is not promotion evidence: real authority adapters, installed host payloads, exact-head generated-host/CI proof and a fresh frontier/low-tier model matrix remain required; June 2026 earns zero current credit and HTML farm use stays disabled. |
| **AC-019** Loss-aware legacy import | Explicit line-mapped pair import is exercised. An exact-candidate native-Windows pilot preserved draft/unverified authority and transferred no approval; human-reviewed mappings and old-approval disposition remain required for production files. |
| **AC-020** Recoverable pair cutover and rollback | Journaled pair cutover/guarded rollback runs on Linux, with native Windows storage primitives separately qualified. The exact-candidate Windows pilot restored both legacy files byte-for-byte and removed both HTML authorities; the full Windows semantic suite, macOS exact-host and production-host recovery remain open. |
| **AC-021** Consumer closure across canonical and generated sources | Partial: the inventory-led closure gate accounts for entry/resume, planning/TDD/execution, context, status/doctor/startup/commit, farm, migration, CI, published instructions, generated copies and explicit exclusions. Entries marked pending still require their named runtime integration and host evidence before default enablement. |
| **AC-022** Trusted native binary distribution | Linux and Windows native builders and pinned bridges pass. The release-only boundary rejects linked/replaced roots, fake architectures and unsupported identities, and requires qualification receipts bound to exact commit, protected workflow run, platform, digest and named native tests. Required CI assembles all three exact-host candidates into package-owned overlays; Linux and Windows cold execution pass from each staged root without Go, PATH or network fallback while the ordinary installer remains fail-closed. Actual release-channel application, hosted macOS and published provenance remain incomplete. |
| **AC-023** Platform-tested write and recovery guarantees | Linux and Windows named process-death/locking/replacement tests pass; macOS compiles and vets but awaits exact-host CI. No physical power-loss or remote-filesystem certification. |
| **AC-024** Safe HTML and adversarial parsing | Fresh Windows conformance rejects duplicate JSON, appended executable script content, renderer drift and remote-reference SVG logos, and re-runs the strict parser/resource/inert-content suites against one exact candidate. Required Linux/macOS exact-head execution and remaining real-host seams are still open. |
| **AC-025** Separated identities and reproducible hashes | A Python stdlib serializer independent of the Go canonicalizer verifies pinned current-schema normative/model/render identities, and a real task-start transition proves revision/model advancement without normative drift. The safe-integer/string canonical profile matches the original contract; hosted exact-head cross-platform qualification remains open. |
| **AC-026** Capability diagnostics and conservative legacy behavior | Explicit engine/bridge failures and legacy isolation have local tests and published guidance. The evidence-backed rollout decision keeps legacy behavior as the default; actual all-host cold-install/production-resume handling remains to exercise. |

`SPEC-COMPLIANCE.json` includes exact original statements and actual local test source references. Read each linked test and its results; a matching name or passing test count is not a completeness proof.
