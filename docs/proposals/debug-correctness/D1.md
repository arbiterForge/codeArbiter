# D1: contract freeze and native-adoption status

Date: 2026-09-25. Same draft PR #871. D1 is partially complete: T01-T04 source preparation is complete; T05 native adoption and T06 actual approval remain blocked. No D2/D3 implementation, generated adapter, runtime validator, canonical artifact or approval event was created.

## Exact source and integration decision

Proposal base: arbiterForge/codeArbiter@f76c457bd8ce56ba765aea16ec7c1b84eefe9d03:docs/proposals/debug-correctness/ . Main rechecked at 4187f1e22dc7da3cf755a2654bbf7ec268236126. PR #854 rechecked open/draft at 578065076e2e11a4e63f33256cb386a5cba1d7d2, not merged. Both debug files were compared as complete Git blobs to the previously reviewed 35a3cd18a327fe5aa306ba452cc21ae096a24b02; they are byte-identical. Its later five-owner consolidation does not repair the debug phase findings or authorize importing unrelated work.

Retain the whole-file SKILL_ENTRY:debug declaration, sole owning skill, host-specific discovery, actual output-relative resource resolution and retirement of new-skill/skill-author. P-854 remains an implementation prerequisite: integrate accepted consolidation when available or obtain an explicit narrow extraction decision. D1 does not merge either PR, stack on the farm branch, recreate a wrapper or restore retired authoring. The independent brownfield campaign remains optional-context input, not a dependency.

## Task results

| Task | Current state | Delivered evidence |
|---|---|---|
| T01 | SOURCE_COMPLETE | Current refs and complete debug-blob equality; integration policy fixed without importing #854. P-854 itself remains unsatisfied. |
| T02 | SOURCE_COMPLETE | Original G1-G8, R01-R15, all 28 acceptance IDs and 41 scenario IDs retained; criteria/task/scenario checks and adversarial findings below. |
| T03 | SOURCE_COMPLETE | D1-PROFILE.md fixes date-time, exact numeric, scalar/whitespace, full-string identity, optional fingerprint and process-result semantics. D1-CASES.json contains explicit primitive controls. |
| T04 | SOURCE_COMPLETE | Each SEM-01 through SEM-19 has a negative and positive control with its real proof layer; original 14 example identities retained; case-specific outcomes and variant requirements frozen. Actual independent oracle review is pending, not invented. |
| T05 | BLOCKED | Installed ca 2.21.12 engine responds, but its bridge lacks current ArtifactClient.workflow_preflight. No native pair exists. |
| T06 | BLOCKED | Source adversarial review completed here; current native ready pair, expanded native task records, independent review and actual host-observed approval have not occurred. |

SOURCE_COMPLETE denotes proposal preparation only. It is not an engine ACCEPTED state, a passed product scenario or authority to start D2/D3. D1's requested exit remains unmet.

## T02/T06 source adversarial pass

Even complete criterion-to-task mapping can pass while the real fix consumer ignores the handoff, a solvable model case escapes unresolved, or an evaluator solution leaks into the solver context. The freeze therefore keeps actual consumer/host/model evidence separate and makes allowed dispositions variant-specific. Evaluator-side metadata must remain outside the synthetic solver checkout and accessible tools. Any detected solution leakage invalidates that trial; a private filename alone is not isolation.

The numeric contract was incomplete without rounding-sensitive controls: default float parsing can turn 1e-4000 into zero or a long fractional number into an integer. D1 freezes exact decimal meaning without an arbitrary new numeric range or exponent-sized allocation. Timestamp support is an explicit subset rather than an unqualified RFC3339 claim. Unknown-offset -00:00 remains distinguishable from a known local offset. Scalar counts and UTF-8 byte counts are separate.

The optional fingerprint could accidentally become an expensive new prerequisite or false proof. Initial production is explicitly omitted; retained pairs never bypass targeted source/index/runtime checks. No generic recipe executor, hash subsystem or persistent case store is added.

A no-effect validator cannot promise an envelope after process death or broken stdout. The normal envelope and abnormal caller behavior are now explicit. Unknown input keys cannot enter diagnostic paths and leak rejected data. The helper does not gain process-supervision or authority duties merely to make a negative test pass.

A matrix mapping SEM rules to D checks alone would overclaim causality, disclosure safety, permissions and currentness. D1-CASES.json names D/H/M boundaries separately; hostile-looking inert text is not automatically rejected if structurally valid. Correct acceptance with zero effects is an essential positive control.

No further missing user-owned product requirement was identified in the source-only scope pass. That is not a finding of zero runtime defects or an independent review receipt. Native review and executable fixtures can still reject this design.

## Native capability evidence and remaining boundary

D1-ENVIRONMENT.json records the installed bridge and binary digests. A real read-only capabilities operation through that installation-pinned bridge returned binary version 0.1.0, schema 0.3.1 and windows/amd64. The measured executable digest matched its installed release manifest. This is binary capability evidence only, not a claim that the installed package satisfies today's workflow.

Current source requires workflow_preflight in addition to binary capabilities: arbiterForge/codeArbiter@4187f1e22dc7da3cf755a2654bbf7ec268236126:core/surface/includes/artifacts.md, Installed host admission. The inspected bridge has no such method. No newer sibling package, source-built binary, fake capability or authority source was substituted; no plugin setting, trust, cache installation or credential was changed. A separate remote checkout action was blocked by the execution tool and was not repeated through another checkout mechanism. Subsequent source processing read exact Git objects only.

Resume T05 in a supported native authoring session using a matching qualified installed package and the intended PR branch. Recheck current main/#854 and all source digests, perform real installed workflow admission, create the native pair with external-ID correspondence, validate ready and preserve exact binding. Then perform the actual review/approval required by T06. A generic D1 request, this document or a capability response is not the native approval reply. Do not invent artifact IDs, populate approval receipts or mark D1 done.

## Verification boundary

The accompanying source-preparation check verifies frozen primitive controls, complete SEM/example/AC/scenario inventories, dependency consistency and descriptive task states. It is not the proposed production validator and imports no product code. All 41 product scenarios, installed debug/fix journeys and model trials remain pending. The read-only binary probe does not qualify those journeys. No live model/provider trial or expenditure was initiated.

Project bootstrap snapshots remain historical; no organization canon, cross-product claim or release status is changed. Rollback removes this D1 proposal increment or reverts its explicit document edits, retaining the earlier review and all unrelated #854 work. Native artifacts require no rollback because none were created.

## Executed preparation checks

`python -B docs/proposals/debug-correctness/check-d1.py` passed all seven source-only test methods, including 59 primitive vectors, all 19 semantic-rule design rows, all 41 scenario records, 40 task identities, complete 28-criterion coverage and an acyclic dependency graph. An unsupported selector was rejected with exit 2, not an empty passing run. No bytecode directory was created in proposal staging. Exact final checked-file hashes and captured runner output are in D1-CHECKS.json; this is not product validation or proof of host-wide zero effects.
