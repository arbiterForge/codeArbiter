{
  "name": "codearbiter-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Professional codeArbiter product documentation with source-backed generated reference.",
  "scripts": {
    "gen": "tsx scripts/gen.ts && npm run gen:diagrams && npm run audit:diagrams",
    "gen:diagrams": "tsx scripts/generate-diagrams.ts",
    "gen:readme-hero": "tsx scripts/generate-readme-hero.ts",
    "capture:proof": "tsx scripts/capture-hook-proof.ts",
    "predev": "npm run gen",
    "dev": "astro dev",
    "prebuild": "npm run gen",
    "build": "astro build",
    "preview": "astro preview",
    "pretest": "npm run gen",
    "test": "vitest run && npm run test:academy-base && npm run test:release-applicability-rendered",
    "test:academy-base": "node scripts/test-academy-non-root-base.mjs",
    "test:release-applicability-rendered": "astro build && tsx test/release-applicability/rendered.ts",
    "test:academy-source": "vitest run scripts/academy-source.test.ts",
    "test:browser": "playwright test",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "link-audit": "tsx scripts/link-audit.ts",
    "audit:diagrams": "tsx scripts/diagram-audit.ts",
    "precoverage": "npm run gen",
    "coverage": "vitest run --coverage && npm run test:academy-base && npm run test:release-applicability-rendered"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.1",
    "@astrojs/starlight": "^0.41.1",
    "astro": "^7.0.3"
  },
  "optionalDependencies": {
    "sharp": "^0.35.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.63.0",
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "4.1.9",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "4.1.9"
  }
}
