---
title: Gated workflows for Claude Code
description: Gated software-engineering workflows for Claude Code.
template: splash
hero:
  tagline: |
    Orchestrate every Claude Code request through a gated lane.
    Tests, reviews, and security checks are real stops: not suggestions,
    not optional, not bypassed under time pressure.
  actions:
    - text: Get started
      link: ./overview/
      icon: right-arrow
      variant: primary
    - text: How it works
      link: ./concepts/
      icon: open-book
      variant: minimal
---

import GateCatchTerminal from '../../components/GateCatchTerminal.astro';
import ForgeShowcase from '../../components/ForgeShowcase.astro';

<div class="ca-terminal-section">

## A gate catching a real mistake

The conversion mechanic is not a description of gates. It is a gate doing its job.

<GateCatchTerminal />

</div>

<div class="ca-hero">
  <div class="ca-hero__what">
    A Claude Code plugin. One orchestrator. Gated lanes for every kind of work.
  </div>

  <div class="ca-hero__first-command">

```bash
/ca:feature "add rate limiting"   # brainstorm → plan → TDD → review → PR
/ca:fix "login 500 on empty body" # test-first bug fix, gate before commit
/ca:commit                        # the only sanctioned path to a commit
```

  </div>
</div>

<div class="ca-laneflow">

<figure class="ca-diagram">
  <img
    src="/codeArbiter/diagrams/lane-flow.svg"
    alt="Lane flow: a command routes to the owning skill, clears its gate pillars, then ships to version control via a pull request."
    loading="lazy"
    width="900"
    height="160"
  />
  <figcaption>Every request flows: command → route → gate → ship. Nothing skips the gate.</figcaption>
</figure>

</div>

<ForgeShowcase />
