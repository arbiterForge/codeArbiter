---
/* codeArbiter override: dark-only.

   The theme switcher is intentionally removed; this override renders nothing in
   its place. ThemeProvider.astro forces `data-theme="dark"`. To restore a light
   theme, revert both overrides AND reinstate the light token block in
   theme.css. */
---
