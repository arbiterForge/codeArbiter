severity: medium

page: guides/uninstalling.md

user_goal: Fully remove codeArbiter after trying it.

gap: "Full Uninstall" step 1 only gives uninstall commands for Claude Code (`claude plugin uninstall ca`) and Codex (`codex plugin remove ca-codex@codearbiter`). No Pi uninstall command, even though README.md references `pi remove` for that host.

remediation: Add the Pi uninstall command to the same list.
