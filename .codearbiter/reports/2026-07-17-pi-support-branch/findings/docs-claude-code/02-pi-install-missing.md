severity: high

page: missing page (no Pi section in getting-started/install.md, no dedicated guide)

user_goal: Install and use codeArbiter with the Pi host.

gap: README.md documents a full Pi install flow (`pi install git:arbiterForge/codeArbiter@ca-pi-v<version>`, supported Pi versions 0.80.5/0.80.6, Node dependency, Git-only distribution) — none of this exists on the docs site. A Pi user following the site alone cannot install codeArbiter at all.

remediation: Add a Pi install section to install.md (mirroring the Codex section) or a new getting-started page, sourced from README.md's existing content.
