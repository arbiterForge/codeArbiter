severity: low

page: getting-started/install.md

user_goal: Know both prerequisites are actually verifiable, not just asserted.

gap: Prerequisites section says "Python 3 on PATH" and "git config user.email set" but gives no verification commands (troubleshooting.md later gives python3 --version / python --version, but that's a different page a first-time installer may not read yet).

remediation: Add the one-line verification commands directly in the Prerequisites list on install.md.
