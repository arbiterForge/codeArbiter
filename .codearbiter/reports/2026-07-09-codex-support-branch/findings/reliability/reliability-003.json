{"schema":"finding/v1","id":"reliability-003","lens":"reliability","title":"load_host() swallows all exceptions and falls back to Claude defaults on a Codex install — a broken _host.py silently un-guards every apply_patch write","category":"reliability","severity":"medium","confidence":0.75,"observed":false,"locations":[{"path":"core/pysrc/hostapi.py","lines":"176-191"},{"path":"core/pysrc/hostapi.py","lines":"150-173"},{"path":"core/pysrc/pre-write.py","lines":"118-127"}],"evidence":"`except Exception: return Host()` — on ca-codex, ANY failure loading _host.py (partial install, corrupted file, an import shadowing) yields the CLAUDE host with zero stderr breadcrumb. Downstream on a Codex payload: base Host.iter_file_ops hits the default branch, `fpath = ti.get(\"file_path\", \"\")` is \"\" (Codex tool_input is {\"command\": \"<patch>\"}), so pre-write._guard_op runs classify_protected(\"\", root) → no classes → every apply_patch (including writes to CONTEXT.md, .markers/, overrides.log, ADRs) is ALLOWED unguarded. The fail-closed 'opaque' path lives only in CodexHost. Also has_statusline reverts to True, so session-start heals the ~/.claude statusline pin from inside a Codex session.","impact":"The documented worst failure shape — silent dormancy — on the enforcement-critical path: the write gate looks installed but admits everything, with no signal. The 'partial install degrades to today's behavior' rationale is only safe for the ca plugin; on ca-codex 'today's behavior' is the WRONG host's semantics.","recommendation":"On load failure, distinguish 'no _host.py' (legitimate Claude fallback) from '_host.py present but failed to load': the latter should warn loudly on stderr and, for PreToolUse entries, fail CLOSED (or at minimum emit the H-00-style breadcrumb) rather than silently adopting Claude defaults.","acceptance_criteria":["With a syntactically broken plugins/ca-codex/hooks/_host.py, pre-write on an apply_patch payload targeting .codearbiter/CONTEXT.md blocks (or at minimum emits a loud stderr warning), never a silent allow","A missing _host.py still degrades to Host() unchanged for the ca plugin"],"effort":"S","depends_on":[],"dedup_key":"reliability:core/pysrc/hostapi.py:load-host-silent-claude-fallback","created_at":"2026-07-09T00:00:02Z"}