{"schema":"finding/v1","id":"architecture-001","lens":"architecture","title":"Remove or wire the dead `host` parameter on all 20 run(host) entry points — the seam handle is cosmetic","category":"architecture","severity":"medium","confidence":0.95,"observed":true,"locations":[{"path":"core/pysrc/pre-write.py","lines":"153-164"},{"path":"core/pysrc/pre-edit.py","lines":"205-216"},{"path":"core/pysrc/_hooklib.py","lines":"76-84"},{"path":"core/pysrc/session-start.py","lines":"541-556"}],"evidence":"Every one of the 20 entry scripts defines `def run(host, argv=None)` per ADR-0011's 'importable run(host) functions' shape, but a mechanical scan of all 20 run() bodies shows NONE references `host` — each delegates to main(), which re-acquires the host through the hidden module-global `_hooklib.get_host()` / a fresh `hostapi.load_host()` (e.g. session-start.py:541 calls `hostapi.load_host()` inside main, ignoring the run() argument). pre-write.py:153-160: `def run(host, argv=None): ... main(); return 0`.","impact":"The parameter promises dependency injection it does not deliver: `run(fake_host)` silently runs against the disk-loaded _host.py instead, so contract tests exercising 'same entry, two hosts' through run() would be testing the wrong host without failing. It also masks that the real seam wiring is a global singleton, inviting a third-host author to assume the parameter is live.","recommendation":"Either make the parameter load-bearing (thread `host` into main()/the guard bodies, or have run() seed the _hooklib._HOST cache from its argument) or drop it to `run(argv=None)` and amend the ADR-0011 entry-shape wording. Seeding the cache is the minimal change that makes test injection honest.","acceptance_criteria":["Calling run() with a stub Host whose project_root()/iter_file_ops() differ observably changes the entry's behavior, or the parameter no longer exists","No entry script re-loads the host independently of the run() argument"],"effort":"S","depends_on":[],"dedup_key":"architecture:core/pysrc:run-host-param-dead","created_at":"2026-07-09T00:00:00Z"}