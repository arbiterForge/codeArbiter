{"schema":"finding/v1","id":"infra-003","lens":"infra","title":"license-consistency CI check never examines ca-codex's plugin.json, so a license drift on the new plugin ships undetected","category":"reliability","severity":"medium","confidence":0.85,"observed":true,"locations":[{"path":".github/scripts/check_license_consistency.py","lines":"37-41"},{"path":".github/workflows/ci.yml","lines":"408-423"},{"path":"plugins/ca-codex/.codex-plugin/plugin.json","lines":"7"}],"evidence":"check_license_consistency.py's `MANIFESTS` list is hardcoded to `plugins/ca/.claude-plugin/plugin.json` and `plugins/ca-sandbox/.claude-plugin/plugin.json` (lines 39-40); ca-codex's manifest lives at a different, un-listed path, `plugins/ca-codex/.codex-plugin/plugin.json`, which currently declares `\"license\": \"AGPL-3.0-only\"` matching the canonical value -- but the check does not read that file at all, so nothing enforces it stays matched. The CI job `license-consistency` (ci.yml:408-423) has no `if:` gate (always runs) and is comment-documented as guarding exactly this class of drift (\"a partial relicense can't ship... the v2.6.0 relicense left three surfaces stale, caught only by human review\") -- ca-codex is now a fourth manifest surface outside that net.","impact":"A future relicense (or an accidental license-field typo/downgrade) on ca-codex's plugin.json passes CI silently, reintroducing the exact stale-declaration failure mode the check was built to prevent, but now on the newest of the three plugin manifests.","recommendation":"Add `plugins/ca-codex/.codex-plugin/plugin.json` to check_license_consistency.py's MANIFESTS list (the check_manifest_agreement logic is already SPDX-family-generic and path-driven, so this is a one-line addition plus verifying the differing manifest directory name `.codex-plugin` vs `.claude-plugin` is handled).","acceptance_criteria":["check_license_consistency.py reads and validates plugins/ca-codex/.codex-plugin/plugin.json's license field against the canonical SPDX","a deliberately mismatched ca-codex license value fails the license-consistency CI job in a local dry run"],"effort":"S","depends_on":[],"dedup_key":"infra:.github/scripts/check_license_consistency.py:ca-codex-manifest-not-checked","created_at":"2026-07-09T00:00:00Z"}
