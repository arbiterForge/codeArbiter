# Implementation plan — Release-contract closure

**Status:** APPROVED — explicit user approval on 2026-09-21, including authorization to proceed to execution. Amended 2026-09-22 by the user's explicit direction to reject acceptance/override of HIGH-T23R2-1, fix it, and commit the corrected effort; R-06/T-25 are that bounded continuation.

**Spec:** `.codearbiter/specs/release-contract-closure.md`

## Acceptance-criterion ledger

- **AC-01:** Every distinct finding in #570 (incl. all 13 comment rounds), #623, #626, #627 gets an explicit disposition; #570's two remote-dependent states and back-fill's hardcoded name are named as deferred.
- **AC-02:** `release_tree_status()` returns a failed probe with an actionable diagnostic past 30s — never an uncaught `TimeoutExpired`, never a false mutation, never success.
- **AC-03:** Clean/dirty probes keep return code 0; a probe failure routes through the existing probe-failure path; operator commands never inherit the internal timeout.
- **AC-04:** `releasehash` malformed usage returns 64, unknown target returns 65, both documented and tested.
- **AC-05:** Confirmation digests/markers/target-separation/read-before-record are byte-identical; no marker on bad input.
- **AC-06:** The CI shim documents the real `select-target-named` CLI route, not the absent importable wrapper.
- **AC-07:** Duplicate payload basenames (equal or different prefixes) are rejected before a map entry is overwritten.
- **AC-08:** Missing/malformed/unreadable declarations fail cleanly at the version gate, no traceback, no default map.
- **AC-09:** A non-ancestor/unresolvable baseline stops before any release edit; ancestor and zero-tag paths unaffected.
- **AC-10:** Missing commit-gate/branch prerequisites stop before any tracked-file write, with an accurate next action.
- **AC-11:** Back-fill's "never again" claim is checkout/file-scoped; branch refusal precedes the declaration write.
- **AC-12:** `feat!`/`feat(scope)!`/`BREAKING CHANGE:` are visibly marked in composed notes; first breaking entry wins the major-bump headline.
- **AC-13:** Phase 2 becomes named, ordered substeps with every existing STOP/BLOCK point preserved.
- **AC-14:** Every retained obligation/assertion maps to its new location and a proving test; nothing deleted or weakened.
- **AC-15:** Shared mechanisms change only at their canonical owner and regenerate every host projection; CI-only fixes stay CI-only.
- **AC-16:** Fresh named-test outcomes against identified candidate bytes; no test silently runs against stale HEAD.
- **AC-17:** No change to publisher/version-boundary behavior; only actually-changed targets advance.
- **AC-18:** A real bounded blind exercise (≤2 rounds, P8) proves the exact rendered candidate; #570 residuals never described as fixed; an open PR never described as shipped.
- **AC-19:** Back-fill declares, authors, validates, and publishes an exact-SHA reconciliation ledger before release re-entry; the real pre-footer first-release route passes without rewriting history or weakening the footer gate.

## Ordered tasks

| id | path(s) | verification | maps-to | covers | depends-on | status |
|---|---|---|---|---|---|---|
| T-01 | `.codearbiter/reports/release-closure-triage.md` (new) | Report enumerates every distinct finding across #570 (all 13 rounds)/#623/#626/#627 with a disposition (fixed-here / already-fixed-elsewhere+commit / explicitly-deferred); no unassigned finding. Named floor that MUST each get their own line (not a bucketed summary) — already known from Phase-1 triage, not exhaustive: the two remote-dependent states (`gh release view`/`gh release list` unreachable without a configured remote); back-fill's hardcoded `app` name / `prefix: v`; the `commit-gate`-required-in-a-fresh-consumer tension; the missing stdlib TOML writer for the manifest-update step; the missing per-commit `classify` CLI subcommand; `commands/release.md`'s drift from `SKILL.md`; the missing `semver-max` helper. Every commit-hash citation for an already-fixed item is spot-checked with `git show <hash> --stat`, not merely copied from a prior comment | TDD obligations AC-01 | AC-01 | — | ACCEPTED |
| T-02 | `core/pysrc/_releaselib.py`; `.github/scripts/test_release_lib.py` | `python .github/scripts/test_release_lib.py -k ReleaseTreeStatusTimeout` fails before the fix, passes after — reuse the `timeout=30`+`TimeoutExpired`-catch design from #627's 2026-09-17 comment verbatim, do not re-derive | TDD obligation AC-02 | AC-02 | T-01 | ACCEPTED |
| T-03 | `.github/scripts/test_release_lib.py`; `core/pysrc/_releaselib.py` | `python .github/scripts/test_release_lib.py -k TreeStatus -k RunPreTag` passes (corrected from an invalid `-k "X or Y"` single-flag form during T-03's own execution — stdlib `unittest -k` is `append`-based, not boolean-string-parsed; the two-flag form is the actual working equivalent): clean/dirty/failed-probe cases, `run-pre-tag`'s probe-failure path at both call sites (not mutation path), operator-command timeout non-inheritance | TDD obligation AC-03 | AC-03 | T-02 | ACCEPTED |
| T-04 | `plugins/ca/hooks/_releaselib.py`; `plugins/ca-codex/hooks/_releaselib.py`; `plugins/ca-pi/hooks/_releaselib.py` | `python tools/sync-core.py --check` passes; generated copies exercise identical timeout behavior | TDD obligations AC-15, AC-16 | AC-15, AC-16 | T-03 | ACCEPTED |
| T-05 | `core/pysrc/releasehash.py`; `.github/scripts/test_release_lib.py` | `python .github/scripts/test_release_lib.py -k ReleasehashExitCodes` passes: usage=64, unknown-target=65, existing 0/1/2(never-confirmed)/3/4 unchanged; no marker written on bad input | TDD obligations AC-04, AC-05 | AC-04, AC-05 | T-03 | ACCEPTED |
| T-06 | `core/surface/skills/release/SKILL.md`; `.github/scripts/test_release_lib.py` | `python .github/scripts/test_release_lib.py -k ReleasehashCallers` passes: caller inventory confirms usage/unknown-target failures never route to record-confirmation; legitimate first confirmation unaffected | TDD obligations AC-04, AC-05 | AC-04, AC-05 | T-05 | ACCEPTED |
| T-07 | `.github/scripts/_releaselib.py`; `.github/scripts/test_release_lib.py` | `python .github/scripts/test_release_lib.py -k ShimDocumentedApi` passes: doc corrected, `select-target-named` + real exports covered, no wrapper added | TDD obligation AC-06 | AC-06 | T-06 | ACCEPTED |
| T-08 | `.github/scripts/payload_version_gate.py`; `.github/scripts/test_payload_version_gate.py` | `python .github/scripts/test_payload_version_gate.py -k BasenameCollision` passes: equal- and different-prefix collisions both refused before overwrite; existing non-colliding mapping unchanged | TDD obligation AC-07 | AC-07 | T-07 | ACCEPTED |
| T-09 | `.github/scripts/payload_version_gate.py`; `.github/scripts/test_payload_version_gate.py` | `python .github/scripts/test_payload_version_gate.py -k DeclarationError` passes: missing/malformed/unreadable declarations fail cleanly with path+cause, no traceback, `ca-pi`'s separate gate untouched | TDD obligation AC-08 | AC-08 | T-08 | ACCEPTED |
| T-10 | `core/pysrc/_releaselib.py`; `core/surface/skills/release/SKILL.md`; `.github/scripts/test_release_lib.py` | `python .github/scripts/test_release_lib.py -k AncestryRefusal` passes: ancestor passes, non-ancestor/unresolvable refuses with tag+cause, zero-tag path unaffected | TDD obligation AC-09 | AC-09 | T-06 | ACCEPTED |
| T-11 | `.github/scripts/test_release_lib.py`; `.github/scripts/test_consumer_smoke.py` | `python .github/scripts/test_release_lib.py -k AncestryGraphs` and `python .github/scripts/test_consumer_smoke.py -k Ancestry` pass: disposable-repo fixtures for sibling branch, decoy sibling series, annotated tags, zero tags; refusal precedes any manifest/changelog/tag mutation | TDD obligations AC-09, AC-16 | AC-09, AC-16 | T-10 | ACCEPTED |
| T-12 | `core/surface/skills/release/SKILL.md`; `core/surface/commands/release.md`; `.github/scripts/test_release_lib.py`; `.github/scripts/test_consumer_smoke.py` | `python .github/scripts/test_release_lib.py -k PrerequisiteRefusal` and `python .github/scripts/test_consumer_smoke.py -k Prerequisite` pass: zero-state consumer stops before tracked-file edits with accurate next action; prepared consumer's route unaffected | TDD obligation AC-10 | AC-10 | T-11 | ACCEPTED |
| T-13 | `core/surface/skills/release/SKILL.md`; `.github/scripts/test_release_lib.py`; `.github/scripts/test_consumer_smoke.py` | `python .github/scripts/test_release_lib.py -k BackfillOrdering` and `python .github/scripts/test_consumer_smoke.py -k Backfill` pass: default/protected-branch refusal precedes the declaration write; checkout-scoped wording verified; existing-file/malformed-file refusal preserved | TDD obligation AC-11 | AC-11 | T-12 | ACCEPTED |
| T-14 | `core/pysrc/_releaselib.py`; `.github/scripts/test_release_lib.py` | `python .github/scripts/test_release_lib.py -k BreakingClassification` passes: `feat!`, `feat(scope)!`, and a `BREAKING CHANGE:` footer all distinguished from nonbreaking; aggregate bump unchanged; reuses `classify_commit`/`classify_window`, no second parser | TDD obligation AC-12 | AC-12 | T-13 | ACCEPTED |
| T-15 | `core/surface/skills/release/SKILL.md`; `core/surface/commands/release.md`; `.github/scripts/test_release_lib.py` | `python .github/scripts/test_release_lib.py -k BreakingHeadline` passes: fixed examples preserve authored text, expose breaking status, and choose the correct major-bump title; minor/patch fallback intact | TDD obligation AC-12 | AC-12 | T-14 | ACCEPTED |
| R-01 | `core/pysrc/_releaselib.py`; `.github/scripts/test_release_lib.py` | `python .github/scripts/test_release_lib.py -k VerifyTagAncestorArgvGuard` passes: a leading-dash or NUL-containing `tag`/`candidate` is rejected before reaching `subprocess.run`'s argv (mirrors `_committed_changelog_text`'s existing `startswith("-")`/`"\0" in` guard), and/or `--` is inserted before the two revisions in the `git merge-base --is-ancestor` argv; existing ancestor/not-ancestor/unresolvable classifications for ordinary tag names are unchanged | Scope-C security review finding 1 (MEDIUM) | AC-09 | T-15 | ACCEPTED |
| R-02 | `core/pysrc/_releaselib.py`; `core/surface/skills/release/SKILL.md`; `.github/scripts/test_release_lib.py`; `.github/scripts/test_consumer_smoke.py` | `python .github/scripts/test_release_lib.py -k AncestryRepoRootBinding` and `python .github/scripts/test_consumer_smoke.py -k AncestryRepoRootBinding` pass: `verify-tag-ancestor` and the adjacent `git tag -l`/`git fetch --tags`/`git branch --show-current` calls in SKILL.md's Pre-flight sequence are proven to resolve against the SAME repo root even when `CLAUDE_PROJECT_DIR` and the invoking shell's cwd name different checkouts (a real scenario in this repo's own linked-worktree setup) — either by pinning every bare git command to one resolved root (`git -C "$PROJECT_ROOT"`) or by threading an explicit root argument through `verify-tag-ancestor` from the same value used to gather `$LAST_TAG`'s input | Scope-C security review finding 2 (MEDIUM) | AC-09 | R-01 | ACCEPTED |
| R-03 | `core/surface/skills/release/SKILL.md`; `.github/scripts/test_release_lib.py` | `python .github/scripts/test_release_lib.py -k BreakingFooterCompleteness` passes: footer-completeness (step 3) requires a `CHANGELOG:` footer on ANY commit where `breaking` is true, regardless of type (not just `feat`/`fix`/`perf`/`refactor`) — closing the path where an unfootered `chore!:`/`docs!:`/etc. forces a major bump but leaves `### Breaking` empty, so `<summary>`'s major branch would otherwise silently fall through to a non-breaking-looking bullet; existing footer-completeness behavior for the original four types is unchanged | Scope-C security review finding 3 (MEDIUM) | AC-12 | R-02 | ACCEPTED |
| T-16 | `.codearbiter/reports/release-closure-invariant-map.md` (new); `.github/scripts/test_release_lib.py`; `core/surface/skills/release/SKILL.md` | Report gives every current literal assertion and its owning obligation an explicit disposition (retained/new location + proving test); no test removed for convenience. Floor to reconcile, not just cite: `sed -n '320,346p' core/surface/skills/release/SKILL.md \| grep -c "MUST\|STOP\|BLOCK\|exit [0-9]"` currently returns 10 normative markers in the Phase-2 span (as of this plan's authoring, one of which — line 338 — is the single 9,324-character unbroken paragraph #623 names); the report must carry one row per marker found by that same count re-run fresh against the current file, not merely the number 10 assumed unchanged | TDD obligation AC-14 | AC-14 | R-03 | ACCEPTED |
| T-17 | `core/surface/skills/release/SKILL.md`; `core/surface/skills/release/references/tagging-rationale.md` (new, only if useful) | A reader finds one executable zero-tag sequence and the sole permitted tag-creation point without contradictory alternatives; the invariant map accounts for every moved obligation | TDD obligations AC-13, AC-14 | AC-13, AC-14 | T-16 | ACCEPTED |
| T-18 | `.github/scripts/test_release_lib.py`; `.github/scripts/test_consumer_smoke.py`; `.codearbiter/reports/release-closure-invariant-map.md` | `python .github/scripts/test_release_lib.py -k Phase2Structure` and `python .github/scripts/test_consumer_smoke.py -k Phase2` pass: removing each load-bearing revised guard fails its corresponding test; zero-assertion/skipped cases don't count as coverage | TDD obligations AC-13, AC-14 | AC-13, AC-14 | T-17 | ACCEPTED |
| T-19 | `core/surface/commands/release.md`; `plugins/ca/skills/release/SKILL.md`; `plugins/ca/commands/release.md`; `plugins/ca/hooks/_releaselib.py`; `plugins/ca-codex/hooks/_releaselib.py`; `plugins/ca-pi/hooks/_releaselib.py`; `plugins/ca-codex/routines/release/SKILL.md`; `plugins/ca-pi/routines/release/SKILL.md`; `plugins/ca-codex/skills/ca-release/SKILL.md`; `plugins/ca-pi/skills/ca-release/SKILL.md`; `plugins/ca/hooks/releasehash.py`; `plugins/ca-codex/hooks/releasehash.py`; `plugins/ca-pi/hooks/releasehash.py` | `python tools/sync-core.py --check` and `python tools/build-surface.py --check` pass; every declared projection/reference resolves and stays consistent; no undeclared scope-widening output | TDD obligation AC-15 | AC-15 | T-18 | ACCEPTED |
| R-04 | `.github/scripts/test_release_lib.py` | `python .github/scripts/test_release_lib.py -k ProjectRootConditionalPerHost` passes: the claude-rendered payload asserts `PROJECT_ROOT=${CLAUDE_PROJECT_DIR:-$(pwd)}`; the ca-codex and ca-pi rendered payloads each assert `PROJECT_ROOT=$(pwd)` AND zero occurrences of the literal `CLAUDE_PROJECT_DIR`; mutation-proven against the three real rendered payloads, not the unconditional template source (which contains both branch strings regardless of host and so cannot catch a future template edit that wrongly references `CLAUDE_PROJECT_DIR` in the Codex/Pi branch) | Scope-D security review finding (MEDIUM) — regression-proofs R-02 | AC-09 | T-19 | ACCEPTED |
| T-20 | `.github/scripts/test_consumer_smoke.py`; `.github/scripts/test_release_lib.py`; `.codearbiter/reports/release-closure-journeys.md` (new) | Full `python .github/scripts/test_consumer_smoke.py` and `python .github/scripts/test_release_lib.py` pass; the spec's fixed scenario matrix is exercised with #570's intended behavior changes recorded separately from #623's behavior-preserving reorg; no live GitHub/npm mutation | TDD obligations AC-09, AC-10, AC-11, AC-12, AC-13, AC-15, AC-16 | AC-09, AC-10, AC-11, AC-12, AC-13, AC-15, AC-16 | R-04 | ACCEPTED |
| T-21 | `.github/scripts/test_ci_impact.py`; `.codearbiter/reports/release-closure-triage.md` | `python .github/scripts/test_ci_impact.py` passes; diff shows `release.yml`, `npm-publish.yml`, `publish-release/action.yml` unchanged | TDD obligation AC-17 | AC-17 | T-09, T-20 | ACCEPTED |
| T-22 | `plugins/ca/.claude-plugin/plugin.json`; `plugins/ca-codex/.codex-plugin/plugin.json`; `plugins/ca-pi/package.json`; `package.json` (generated); `CHANGELOG.md`; `plugins/ca-codex/CHANGELOG.md`; `plugins/ca-pi/CHANGELOG.md`; `.codearbiter/reports/release-closure-journeys.md` | `python tools/build-host-packages.py --check` passes; only targets whose payload actually changed advance version/metadata; unrelated `[Unreleased]` entries preserved | TDD obligations AC-15, AC-16, AC-17 | AC-15, AC-16, AC-17 | T-21 | ACCEPTED |
| T-23 | `.codearbiter/reports/agent-lane-proof.json` (or current supported proof shape); `.codearbiter/reports/release-closure-journeys.md` | `python .github/scripts/check_skill_proof_fresh.py` passes for the exact rendered candidate; helper identities/findings/limitations recorded; a blocker gets exactly one remediation round through its owning task before a second exercise (P8) — never a third, never a severity downgrade | TDD obligation AC-18 | AC-18 | T-22 | **P8 BUDGET EXHAUSTED, AC-18 UNMET — not ACCEPTED, not BLOCKED.** Round 1: HIGH-T23-1 (Back-fill's `$ADOPTED` resolving to its own payload-excluded declaration commit) → remediated as R-05. Round 2 (final, genuinely independent): HIGH-T23R2-1 — R-05's fix correctly surfaces (does not create) a pre-existing, previously-known residual: a Back-fill first-release consumer with real unfootered pre-sprint history has no working remedy today (every path either BLOCKs on footer-completeness, requires rewriting published commits, or needs an undeclared `$CHANGELOG_RECONCILIATIONS` ledger `backfill-detect` never proposes) — this reproduces the campaign's own prior `run_18_HIGH_1` almost verbatim and sits inside the spec's own out-of-scope carve-out (no fully onboarding-free release path for a zero-state consumer). NO-BLOCKER under the artifact's strict data-loss/corruption/silent-wrong-action bar, so P8 does not trigger a BLOCKED sprint — but it is NOT a clean round either, so `proof_current` correctly stays `false` and AC-18's own declared verification does not pass. No third round taken (P8 forbids it); `proof_current` was not hand-flipped (forbidden — a manufactured receipt). Disposition deferred to the user at T-24/PR. |
| R-05 | `core/surface/skills/release/SKILL.md`; `.github/scripts/test_release_lib.py`; `.github/scripts/test_consumer_smoke.py` | `python .github/scripts/test_release_lib.py -k AdoptionBoundaryBackfillFirstRelease` and `python .github/scripts/test_consumer_smoke.py -k AdoptionBoundaryBackfillFirstRelease` pass: Back-fill's canonical first-release path no longer resolves `$ADOPTED` to its own declaration commit when that commit is payload-excluded; the escape-hatch behavior (root-commit fallback → full-history window) becomes the documented default for this exact shape, not an undocumented accident; existing non-Back-fill adoption-boundary behavior (the #570 fix this sits on top of) is unchanged | T-23 round-1 finding HIGH-T23-1 | AC-18 | T-23 | ACCEPTED |
| T-24 | `.codearbiter/reports/release-closure-triage.md`; `.codearbiter/reports/release-closure-invariant-map.md`; `.codearbiter/reports/release-closure-journeys.md` | Every original finding (incl. #570's residuals) has a final disposition; residuals stay visibly unresolved; no completion claim lacks cited evidence; no "shipped" claim on an open PR | TDD obligations AC-01, AC-16, AC-18 | AC-01, AC-16, AC-18 | T-23 | ACCEPTED — final disposition appended to `release-closure-triage.md` §"T-24 — Final disposition and evidence index" (88 findings + 7 named-floor items reconfirmed, 12-row spot-check table; AC-18 unmet stated explicitly with `run_18_HIGH_1` lineage; `test_public_codex_docs.py --require-current-candidate` re-run live by both the subagent and independently by me — same failure, `'0.13.8' != '0.13.7'`, 2/18 failing; #570's two residuals reconfirmed unresolved; explicit "open PR, nothing shipped" line). 4 NEEDS-TRIAGE items harvested to `.codearbiter/open-tasks.md` as `release.debt.0004`–`0007` (one, 0007, flagged unattributed — no verbatim prior citation found, harvested as the subagent's own independently-verified finding instead of a fabricated citation). T-14's classify_window docstring item was NOT harvested — verified already resolved at `core/pysrc/_releaselib.py:1497-1503`. No source files touched (git status confirms only report/open-tasks files changed). |
| R-06 | `core/pysrc/_releaselib.py`; `core/surface/skills/release/SKILL.md`; generated host projections; `.github/scripts/test_release_lib.py` | Regression first fails because `backfill-detect` omits `changelog-reconciliations` and the Back-fill procedure re-enters Pre-flight before publication. After the fix: focused Back-fill tests pass; `validate-reconciliations` uses the strict ledger parser; a disposable Git repository with a published unfootered `feat` passes `classify-window app trunk` only after the generated declaration and operator-authored exact-SHA ledger are committed to simulated `origin/trunk`; malformed/incomplete/unpublished evidence remains covered by the existing reconciliation tests. | User-directed remediation of HIGH-T23R2-1; TDD obligation AC-19 | AC-19 | T-24 | ACCEPTED — red: 5 focused failures for the missing declaration/publication route. Green: 41 focused tests including the real Git fixture; `sync-core.py --check` and `build-surface.py --check` both pass after canonical regeneration. Full-gate and independent verification remain T-25. |
| T-25 | `.codearbiter/reports/agent-lane-proof.json`; `.codearbiter/reports/release-closure-journeys.md`; `.codearbiter/reports/release-closure-triage.md` | One bounded independent verification of the materially changed R-06 candidate records exact helper/skill hashes, attacks HIGH-T23R2-1's route, states limitations, and sets `proof_current` true only if no HIGH remains and the exercised bytes equal the committed candidate. No second post-amendment round. | 2026-09-22 user-authorized P8 amendment; AC-18/AC-19 | AC-18, AC-19 | R-06 | ACCEPTED — exact rendered skill SHA-256 `4eae711068e270818e5b86f00a4e5ccffb196af2d2cde52b8fe8c770647eca6e`; helper/vendored SHA-256 `c3c94cdd3bc433123666a8e132b57811c1742b85330d71e9c0367adfd49eb471`. Real Git reconciliation route passed. Independent coverage audit: 0 CRITICAL/HIGH; one MEDIUM test gap fixed; bounded follow-up: no HIGH/MEDIUM. Final suites: release-lib 677; consumer smoke 107 (1 skipped); release workflow 169; public Codex docs 18; proof freshness and canonical/generated/package checks green. The external `--require-current-candidate` release-only check remains honestly red on the 0.13.8 vs 0.13.7 live-proof boundary. |

## Dependency order and MVP slice

Not a single chain: the `depends-on` column encodes a branch-and-merge DAG, not linear T-01→T-24 order. T-01→T-02→T-03→T-04 (triage, timeout fix+test, semantics, regen) is one spine; T-03→T-05→T-06 (releasehash) is a second; T-06 then forks two independent branches that both touch shared files sequentially rather than in parallel — T-07→T-08→T-09 (CI shim, basename collision, declaration errors) and T-06→T-10→T-11→T-12→T-13→T-14→T-15 (ancestry, prerequisites, back-fill, breaking) — and T-21 is the merge point, depending on both branch tips (T-09 and T-20). T-16→T-17→T-18→T-19→T-20 (Phase-2 map/split/bind/regen/journeys) hangs off T-15. No cycle exists in this graph. Tasks share `.github/scripts/test_release_lib.py` and `core/surface/skills/release/SKILL.md` heavily, so — unlike the `preview-release-contract` plan's T-02/T-03 split — the two branches off T-06 are still authored sequentially rather than genuinely in parallel, per the spec's Approach ("shared source files make unrestricted parallel authoring inappropriate," carried from the handoff's own review-scope note); the DAG records the true dependency, not an instruction to fan the branches out concurrently.

**Known transient state, T-05→T-19 (not a regression):** T-05 changed `core/pysrc/releasehash.py`'s bytes; its vendored copies (`plugins/ca*/hooks/releasehash.py`) are not regenerated until T-19, so `CoreCLITest.test_pre_tag_hash_module_is_vendored_byte_identically` fails on a *full, untargeted* `test_release_lib.py` run from now until T-19 lands — this is expected drift the plan's own DAG produces, confirmed as the sole failure (562 other tests pass). Every task T-06 through T-18 must verify with its own scoped `-k` command exactly as tabulated, not a bare full-suite run, until T-19's regeneration closes this gap. Also carried forward from T-05: testing `releasehash.py`'s CLI (`main()`) in-process inside `test_release_lib.py` hits a module-caching collision with the CI shim's `_releaselib` import (crashes before reaching exit-code logic) — use a real `subprocess.run` against `_RELEASEHASH_PATH`, mirroring the existing `_CORE_RELEASELIB_PATH` pattern, for any test that invokes `main()`.

**Scope D quality review (T-16–T-19 combined diff), 2026-09-22:** `security-reviewer` dispatched given the hosted/local execution boundary and publish-authorization prose under restructure. Gate: PASS at review time (0 CRITICAL, 0 HIGH), 1 MEDIUM — a regression-proofing gap (no pinned test verified R-02's `{{IF:claude}}` conditional renders correctly per-host at the generated-payload level, only at the template level) — remediated as R-04, independently fresh-verified including a mutation round that injects the leak away from the obvious `PROJECT_ROOT=` line to prove the test isn't narrowly anchored. Confirmed positively: both named boundary rules (hosted/local execution; Phase-2/Phase-3 push/publish separation) are byte-identical, unmoved, and now pinned governance rules checked against source + all three renders; T-19's regeneration introduced no hand-authored-looking divergence beyond the declared per-host substitutions. This also finally clears the T-05→T-19 transient state tracked since Scope B — the standing `test_pre_tag_hash_module_is_vendored_byte_identically` failure is gone.

**Scope C quality review (T-10–T-15 combined diff), 2026-09-21:** `security-reviewer` dispatched given the release-authority/version-integrity surface. Gate: PASS at review time (0 CRITICAL, 0 HIGH), 3 MEDIUM findings — all three treated as blocking given this scope's central-safety-property role, and remediated as R-01 (argv-injection-precedent guard on `verify_tag_ancestor`, mirroring `_committed_changelog_text`'s existing pattern), R-02 (bind tag-selection and ancestry-verification to one resolved repo root — the most serious finding, a real gap given this repo's own heavy linked-worktree usage), and R-03 (SKILL.md step-3 prose gap — `classify_window` already enforced the wider footer-completeness rule for any breaking commit regardless of type; only the prose undersold it). All three remediations independently fresh-verified; Scope C is closed inclusive of R-01–R-03.

**Scope B quality review (T-05–T-09 combined diff), 2026-09-21:** `security-reviewer` dispatched given the trust-boundary surface (confirmation authority, release-namespace gating). Gate: PASS, 0 CRITICAL, 0 HIGH. One MEDIUM recorded — vendored `plugins/*/hooks/releasehash.py` and the three plugin `SKILL.md` copies not yet regenerated from T-05/T-06's changes — this is exactly the plan's own documented transient state above, not a new finding; T-19 is its designed remedy and no separate remediation task is added. Confirmed positively: exit-code renumbering can't cause a caller to misread failure as success (every real caller gates on exact zero); `payload_version_gate.py`'s two new exception branches fail closed, never open; AC-05's confirmation digest/marker logic is verified byte-unchanged by diff.

**Review scopes** (informational grouping for `subagent-driven-development`'s two-pass quality review — one pass per combined scope, not a fresh audit after every task): A = T-01–T-04 (timeout mechanism); B = T-05–T-09 (releasehash + CI-shim + version-gate); C = T-10–T-15 (ancestry, prerequisites, back-fill, breaking-visibility); D = T-16–T-19 (Phase-2 restructure + regeneration); E = T-20–T-24 (journeys, CI/publisher check, version reconciliation, final proof, close-out).

**MVP slice:** T-01 through T-04. This is the smallest independently coherent unit — triage plus the timeout mechanism, proven and regenerated to all three hosts. It is not shippable separately from the rest of this plan without an explicit scope/branch decision at the finishing gate (per the spec's P1); reaching it does not authorize an early commit, merge, or publish.
