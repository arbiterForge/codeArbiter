# Checkpoint — 2026-06-13

## Project state

| Field | Value |
|-------|-------|
| Stage | 2 |
| Checkpoint date | 2026-06-13 |
| Sweep type | Read-only checkpoint sweep — surfaces findings, no sign-off, blocks nothing |
| Reviewers | architecture-drift, coverage-auditor (test-audit), security, auth-crypto, dependency, migration |
| Decision-challenger | Not run this sweep — 0 ADRs exist (`.codearbiter/decisions/` absent) |
| Prior checkpoint | `.codearbiter/checkpoints/2026-06-12.md` (continuity reference) |
| Suite status at sweep | All green — Python guard matrix 62, cold-install 131, unittest 233, TS vitest 56 |

## Finding summary

| Reviewer | CRITICAL | HIGH | MEDIUM | LOW |
|----------|----------|------|--------|-----|
| architecture-drift-reviewer | 0 | 0 | 0 | 0 (PASS; 1 [NEEDS-TRIAGE] informational) |
| coverage-auditor | 0 | 1 | 1 | 1 (+ 2 [NEEDS-TRIAGE]/out-of-scope) |
| security-reviewer | 0 | 0 | 2 | 3 |
| auth-crypto-reviewer | 0 | 0 | 0 | 0 (PASS) |
| dependency-reviewer | 0 | 0 | 0 | 2 (PASS gate) |
| migration-reviewer | — | — | — | PASS |
| decision-challenger | — | — | — | Not run (no ADRs) |
| **Total** | **0** | **1** | **3** | **9** |

> Summary counts (per triage report): Total findings 13 | BLOCKS 1 | DEFERRABLE 3 | NON_BLOCKING 9.
> All reviewer gates PASS. No finding is enforced this read-only sweep; would-block status is recorded per finding below.

---

## Dispositions

### BLOCKS — would-block (not enforced this read-only sweep)

| Finding | File:line | Source | Severity | Controlling doc | Would-block |
|---------|-----------|--------|----------|-----------------|-------------|
| H-05 (append-only audit logs: Edit must `startswith` old_string) and H-11 (ADR files editable only via `/adr`, gated on fresh `adr-authoring-active` marker) have ZERO behavioral tests — only a benign cold-install allow noop. A regression inverting `startswith`, mis-compiling regex, or dropping marker-freshness passes CI green. Test-obligation gap on security-enforcement code, not a live exploit. | `plugins/ca/hooks/pre-edit.py:30-48` | coverage-auditor | HIGH | security-controls.md (H-05 / H-11) | YES |

Remediation: add `tests/test_pre_edit.py` — H-05 BLOCK/ALLOW; H-11 BLOCK no-marker / BLOCK stale / ALLOW fresh; arbiter-disabled allow; Windows path variants.

### DEFERRABLE — real, safe to follow up

| Finding | File:line | Source | Severity | Controlling doc | Would-block |
|---------|-----------|--------|----------|-----------------|-------------|
| TLS-scheme validation in `validate()` (:761-766) only covers `plan.meta.apiBaseUrl`; `FARM_API_BASE_URL` env override bypasses the HTTPS check (resolved order ENV → plan.meta → ENV.default), so `FARM_API_KEY` could be sent over cleartext `http://`. | `plugins/ca/tools/farm.ts:68` (runtime :267, :810, :835) | security-reviewer | MEDIUM | security-controls.md "TLS" | No |
| Gate commands from `plan.json` execute verbatim via `cmd.exe /c` / `bash -c`; intended, length-bounded ≤1024, no content allowlist. `plan.json` is effectively executable input; this trust boundary is absent from security-controls.md's boundary-crossings table (same for `FARM_MUTATION_CMD`). **[NEEDS-TRIAGE]** whether the governance doc should own this. | `plugins/ca/tools/farm.ts:154-160` (runGate), :521-527 (mutationCheck MUT.cmd) | security-reviewer | MEDIUM | security-controls.md (boundary-crossings) | No |
| TOCTOU-fix helper hashing sensitive lines and binding the security marker; sole test (case 6c) asserts one message. Untested branches: unborn-branch (:60-64), untracked-file inclusion (:65-67), MAX_UNTRACKED_BYTES skip (:42-43), no-`.codearbiter` early-exit-1 (:74-77), empty-digest write (:85). Helper underpins H-09b/H-10b lock trust. | `plugins/ca/hooks/security-pass.py` (whole file) / `test_hook_guards.py:196-199` | coverage-auditor | MEDIUM | security-controls.md (H-09b / H-10b) | No |

Remediation summary: (1) validate resolved `apiBaseUrl` against HTTPS-or-loopback before first `fetch()`; (2) declare the `plan.json` / `FARM_MUTATION_CMD` boundary in security-controls.md; (3) extend guard matrix / add `tests/test_security_pass.py`.

### NON_BLOCKING — informational

| Finding | File:line | Source | Severity | Would-block |
|---------|-----------|--------|----------|-------------|
| `run()` passes full `process.env` (incl `FARM_API_KEY`) to every spawned gate/mutation/git child. Defense-in-depth: strip `FARM_API_KEY` from child env; pass only to in-process `fetch()`. | `plugins/ca/tools/farm.ts:118` | security-reviewer | LOW | No |
| Temp-file write + `os.replace` can leave `settings.json.tmp` on crash; default temp perms; not secret-bearing. Remediation: try/finally cleanup. Optional. | `plugins/ca/hooks/wire-statusline.py:106-112` (save_settings) | security-reviewer | LOW | No |
| H-05 audit-log truncation guard self-documents known bypasses (triple-chevron, `exec 3>` FD redirection). Disclosed accepted residual risk. Re-confirm acceptance at checkpoint. | `plugins/ca/hooks/pre-bash.py:62, :200` | security-reviewer | LOW | No |
| No CVE audit step defined; tech-stack.md:57-58 says no scanner configured; security-controls.md has no audit command, so CVE gate cannot be satisfied automatically. Remediation: add `npm audit --omit=dev --audit-level=critical` to `plugins/ca/tools` CI and reference in tech-stack.md. User decision. | repo-wide (ca-farm toolchain) | dependency-reviewer | LOW | No |
| `esbuild@0.24.2`, `fsevents@2.3.3` install scripts run native-binary download/build; benign, integrity-pinned. No action. | dependency manifest | dependency-reviewer | LOW | No |
| Incidental `overrides.log` reference is NOT coverage of pre-edit H-05; noted to prevent mis-scoping. No action. | `tests/test_init_codearbiter.py:20` | coverage-auditor | LOW | No |
| **[NEEDS-TRIAGE]** Playwright maintenance tool, not a shipped plugin, not CI-gated. Out of test floor. | `tools/statusline-screenshot.py` (tracked, untested) | coverage-auditor | — | No |
| Gitignored scratch, out of scope. | `tmp/hooktest.py` | coverage-auditor | — | No |
| **[NEEDS-TRIAGE]** Governance decisions captured in tech-stack.md and security-controls.md rather than numbered ADRs (0 ADRs exist; `.codearbiter/decisions/` absent). If the team wants them governed/audited as architecture decisions, `/adr` authoring (user-initiated) would be needed. Informational. | governance docs | architecture-drift-reviewer | — | No |

---

## All findings (full detail)

### Architecture Drift
PASS (0 findings) — 0 ADRs, expected early-project state (stage 2).

One [NEEDS-TRIAGE] informational note: governance decisions live in tech-stack.md and security-controls.md rather than numbered ADRs (`.codearbiter/decisions/` absent). User-initiated `/adr` authoring would be required to govern them as architecture decisions. No drift; informational only.

### Test Audit (coverage-auditor)
- **HIGH (would-block)** — `plugins/ca/hooks/pre-edit.py:30-48`: H-05 (append-only audit logs: Edit must `startswith` old_string) and H-11 (ADR files editable only via `/adr`, gated on fresh `adr-authoring-active` marker) have ZERO behavioral tests; only a benign cold-install allow noop. A regression inverting `startswith`, mis-compiling regex, or dropping marker-freshness passes CI green. Controlling doc: security-controls.md (H-05/H-11). Remediation: add `tests/test_pre_edit.py` (H-05 BLOCK/ALLOW, H-11 BLOCK no-marker / BLOCK stale / ALLOW fresh, arbiter-disabled allow, Windows path variants). Note: test-obligation gap on security-enforcement code, not a live exploit.
- **MEDIUM (deferrable)** — `plugins/ca/hooks/security-pass.py` (whole file) / `test_hook_guards.py:196-199`: TOCTOU-fix helper hashing sensitive lines and binding the security marker; sole test (case 6c) asserts one message. Untested branches: unborn-branch (:60-64), untracked-file inclusion (:65-67), MAX_UNTRACKED_BYTES skip (:42-43), no-`.codearbiter` early-exit-1 (:74-77), empty-digest write (:85). Helper underpins H-09b/H-10b lock trust. Remediation: extend guard matrix / add `tests/test_security_pass.py`.
- **LOW (non-blocking)** — `tests/test_init_codearbiter.py:20`: incidental `overrides.log` reference is NOT coverage of pre-edit H-05; noted to prevent mis-scoping. No action.
- **[NEEDS-TRIAGE]** — `tools/statusline-screenshot.py` (tracked, untested): Playwright maintenance tool, not a shipped plugin, not CI-gated. Out of test floor.
- **Out of scope** — `tmp/hooktest.py`: gitignored scratch.

### Security
- **MEDIUM (deferrable)** — `plugins/ca/tools/farm.ts:68` (runtime :267, :810, :835): TLS-scheme validation in `validate()` (:761-766) only covers `plan.meta.apiBaseUrl`; `FARM_API_BASE_URL` env override bypasses the HTTPS check (resolved order ENV → plan.meta → ENV.default), so `FARM_API_KEY` could be sent over cleartext `http://`. Controlling doc: security-controls.md "TLS". Remediation: validate resolved `apiBaseUrl` against HTTPS-or-loopback before first `fetch()`.
- **MEDIUM (deferrable)** — `plugins/ca/tools/farm.ts:154-160` (runGate), :521-527 (mutationCheck MUT.cmd): gate commands from `plan.json` execute verbatim via `cmd.exe /c` / `bash -c`; intended, length-bounded ≤1024, no content allowlist. `plan.json` is effectively executable input; this trust boundary is absent from security-controls.md's boundary-crossings table (same for `FARM_MUTATION_CMD`). Remediation: declare the boundary in security-controls.md. **[NEEDS-TRIAGE]** whether the governance doc should own this.
- **LOW (non-blocking)** — `plugins/ca/tools/farm.ts:118`: `run()` passes full `process.env` (incl `FARM_API_KEY`) to every spawned gate/mutation/git child. Defense-in-depth: strip `FARM_API_KEY` from child env; pass only to in-process `fetch()`.
- **LOW (non-blocking)** — `plugins/ca/hooks/wire-statusline.py:106-112` (save_settings): temp-file write + `os.replace` can leave `settings.json.tmp` on crash; default temp perms; not secret-bearing. Remediation: try/finally cleanup. Optional.
- **LOW (non-blocking)** — `plugins/ca/hooks/pre-bash.py:62, :200`: H-05 audit-log truncation guard self-documents known bypasses (triple-chevron, `exec 3>` FD redirection). Disclosed accepted residual risk. Re-confirm acceptance at checkpoint.

#### Auth-crypto (sub-reviewer)
PASS. Crypto (sha256; md5 only in sanctioned adversarial fixtures per security-controls.md:18-21), TLS, secrets (`FARM_API_KEY` via `process.env` → Authorization header only), shell injection (explicit argv arrays) all PASS.

#### Dependency (sub-reviewer)
- **LOW (non-blocking)** — repo-wide (ca-farm toolchain): no CVE audit step defined; tech-stack.md:57-58 says no scanner configured; security-controls.md has no audit command, so CVE gate cannot be satisfied automatically. Remediation: add `npm audit --omit=dev --audit-level=critical` to `plugins/ca/tools` CI and reference in tech-stack.md. User decision.
- **LOW (non-blocking)** — `esbuild@0.24.2`, `fsevents@2.3.3`: install scripts run native-binary download/build; benign, integrity-pinned. No action.
- **PASS gate** — License (147/147 approved: MIT 142, Apache-2.0 2, ISC 2, BSD-3-Clause 1), provenance (100% registry.npmjs.org, integrity-pinned, lockfileVersion 3), maintenance, supply chain all PASS.

#### Migration (sub-reviewer)
PASS. Database-free architecture; nothing to review.

### Decision Challenges
Not run this sweep. 0 ADRs exist and `.codearbiter/decisions/` is absent, so there are no architecture decisions to challenge. See the architecture-drift [NEEDS-TRIAGE] note above: if governance decisions in tech-stack.md / security-controls.md are to be challenged as ADRs, user-initiated `/adr` authoring would first be required.
